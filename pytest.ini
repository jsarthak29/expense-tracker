[pytest]
pythonpath = .
testpaths = tests
filterwarnings =
    error
    # SQLite has no native DECIMAL. SQLAlchemy round-trips it correctly and warns;
    # Postgres, which this service actually targets, does support it.
    ignore:.*does \*not\* support Decimal objects natively.*:Warning
