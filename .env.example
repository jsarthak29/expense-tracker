# Postgres (what docker-compose.yml brings up, and what the service targets by default)
DATABASE_URL=postgresql+psycopg://expenses:expenses@localhost:5432/expenses

# Any SQLAlchemy URL works. SQLite is handy for a zero-dependency local run:
# DATABASE_URL=sqlite:///./expenses.db
